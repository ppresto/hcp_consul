---
apiVersion: consul.hashicorp.com/v1alpha1
kind: IngressGateway
metadata:
  name: ingress-gateway
spec:
  listeners:
    - port: 9090
      protocol: http
      services:
        - name: frontend
          hosts: ["*"]
    - port: 8080
      protocol: http
      services:
        - name: web
          hosts: ["*"]